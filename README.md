# Бот для учета доходов

Telegram бот для учета и отслеживания доходов по категориям.

## Возможности

- ➕ Добавление доходов по категориям
- 📊 Статистика по месяцам
- 📈 Общая статистика
- ➕ Добавление собственных категорий
- 📅 Добавление доходов за прошедшие даты

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram:
   - Напишите `/newbot` боту @BotFather
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен

3. Создайте файл конфигурации:
```bash
cp config.py.example config.py
```

4. Откройте файл `config.py` и замените `YOUR_BOT_TOKEN` на ваш токен:
```python
BOT_TOKEN = "ваш_токен_здесь"
```

## Запуск

```bash
python bot.py
```

## Использование

1. Запустите бота командой `/start`
2. Используйте кнопки для навигации:
   - **➕ Добавить** - добавить доход по категории
   - **📊 Статистика** - просмотреть статистику
   - **➕ Добавить категорию** - создать новую категорию

## Структура проекта

- `bot.py` - основной файл бота с обработчиками
- `database.py` - работа с базой данных SQLite
- `config.py` - конфигурация (токен бота)
- `requirements.txt` - зависимости проекта
- `income_bot.db` - база данных (создается автоматически)

## База данных

База данных SQLite создается автоматически при первом запуске. Она содержит:
- Таблицу категорий (включая стандартные: ПТТ, ПРИОРИТЕТ, СТАНКИ, СКИПЕТР)
- Таблицу транзакций с датами и суммами

## Развертывание на сервере

Для развертывания на сервере можно использовать:
- systemd service
- screen/tmux
- Docker
- Supervisor

Пример systemd service файла (`/etc/systemd/system/income-bot.service`):
```ini
[Unit]
Description=Income Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/KaznaBot
ExecStart=/usr/bin/python3 /path/to/KaznaBot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable income-bot
sudo systemctl start income-bot
```
